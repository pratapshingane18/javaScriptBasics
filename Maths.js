const num1 = 100;
const num2 = 50;
let val;

// Simple Math with number
val = num1 + num2;
val = num1 * num2;
val = num1 - num2;
val = num1 / num2;
val = num1 % num2;

// Math Object

val = Math.PI;
val = Math.E;
val = Math.round(2.34567);
val = Math.ceil(4.3);
val =Math.floor(2.3);
val = Math.sqrt(64);
val = Math.abs(-3);
val = Math.pow(8,3);
val = Math.min(1,2,3,-3);
val = Math.max(1,2,3,4)
val = Math.random();
val = Math.floor(Math.random() * 20+1) ;





console.log(val);
